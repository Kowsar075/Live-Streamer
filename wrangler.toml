# Cloudflare Pages configuration.
# `pages_build_output_dir` tells Wrangler where the built static site is, so
# `wrangler pages dev` / `wrangler pages deploy` need no extra directory arg.
name = "m3u8-streamer"
pages_build_output_dir = "dist"
compatibility_date = "2024-11-01"
